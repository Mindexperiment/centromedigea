<section class="py-8 px-2">
    <h3 class="mb-4 font-medium text-2xl">{{ __( "Il metodo" ) }}</h3>

    <p class="mb-4">Il <strong>metodo Pilates</strong> o semplicemente <strong>Pilates</strong> è un vero e proprio allenamento che ha come prerequisiti fondamentali:</p>

    <ul class="mb-4">
        <li class="mb-2"><span class="font-medium">-</span> il corretto <strong>ripristino della postura</strong> (in stazione eretta, da seduti, in quadrupedia, supini e proni)</li>
        <li class="mb-2"><span class="font-medium">-</span> la <strong>consapevolezza di ogni movimento</strong> che ci si appresta ad eseguire</li>
        <li><span class="font-medium">-</span> l'<strong>attivazione della muscolatura profonda</strong></li>
    </ul>

    <img src="{{ asset('images/partners/pilates/metodo.jpg') }}" alt="Figura posturale" class="rounded" />

    <div>
        <p class="mb-4 font-medium text-xl">{{ __( "Principi posturali" ) }}</p>

        <ul class="mb-4 list-disc list-inside">
            <li class="mb-2">{{ __( "posizione del bacino" ) }}</li>
            <li class="mb-2">{{ __( "allungamento assiale della colonna vertebrale" ) }}</li>
            <li class="mb-2">{{ __( "posizione delle costole e del torace" ) }}</li>
            <li class="mb-2">{{ __( "posizione del cingolo scapolare" ) }}</li>
            <li class="mb-2">{{ __( "posizione del collo" ) }}</li>
        </ul>

        <p class="mb-4 font-medium text-xl">{{ __( "Principi tradizionali" ) }}</p>

        <ul class="mb-4 list-disc list-inside">
            <li class="mb-2">{{ __( "concentrazione" ) }}</li>
            <li class="mb-2">{{ __( "controllo" ) }}</li>
            <li class="mb-2">{{ __( "respirazione" ) }}</li>
            <li class="mb-2">{{ __( "centralizzazione" ) }}</li>
            <li class="mb-2">{{ __( "precisione" ) }}</li>
            <li class="mb-2">{{ __( "fluidità" ) }}</li>
            <li class="mb-2">{{ __( "isolamento" ) }}</li>
            <li class="mb-2">{{ __( "routine (ripetizione)" ) }}</li>
        </ul>
    </div>
</section>
